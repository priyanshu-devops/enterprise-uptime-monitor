# Base URL of the backend API (no trailing slash, without /api/v1)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Base URL of the GitHub Pages static cache (no trailing slash)
NEXT_PUBLIC_PAGES_BASE_URL=https://example.github.io/uptime-platform
